apply plugin: 'java-library'

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation files('libs/pluginif.jar')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
